const config = {
    plugins: {
        'postcss-import': {},
        'postcss-nested': {},
        'postcss-current-selector': {},
        autoprefixer: {},
        tailwindcss: './tailwind.config.js'
    }
}

module.exports = config
